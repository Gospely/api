var util = require('../utils.js');

var dockers = {};

//
// dockers.extend = function*() {
//
// 	var extend = this.query;
//
// 	if (extend.unit = 'GB') {
// 		extend.size = (extends * 1024 * 1024) / 512;
// 	} else {
// 		extend.size = extends * 1024
// 	}
//
// 	var result = shells.extendsVolume(extends);
//
// 	if (result == 'success') {
// 		this.body ==
// 	}
// }

module.exports = dockers;
